### Crear proyecto
curl -X POST http://localhost:3000/api/project \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "nombre": "Proyecto Demo",
    "descripcion": "Proyecto para test",
    "clientId": "ID_DEL_CLIENTE"
  }'

### Obtener todos los proyectos
curl -X GET http://localhost:3000/api/project \
  -H "Authorization: Bearer $TOKEN"

### Obtener un proyecto por ID
curl -X GET http://localhost:3000/api/project/ID_DEL_PROYECTO \
  -H "Authorization: Bearer $TOKEN"

### Actualizar proyecto
curl -X PUT http://localhost:3000/api/project/ID_DEL_PROYECTO \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "nombre": "Proyecto Actualizado",
    "descripcion": "Descripción actualizada"
  }'

### Archivar proyecto
curl -X PATCH http://localhost:3000/api/project/ID_DEL_PROYECTO/archive \
  -H "Authorization: Bearer $TOKEN"

### Eliminar proyecto (hard delete)
curl -X DELETE http://localhost:3000/api/project/ID_DEL_PROYECTO \
  -H "Authorization: Bearer $TOKEN"

### Obtener proyectos archivados
curl -X GET http://localhost:3000/api/project/archived/list \
  -H "Authorization: Bearer $TOKEN"

### Recuperar proyecto archivado
curl -X PATCH http://localhost:3000/api/project/ID_DEL_PROYECTO/recover \
  -H "Authorization: Bearer $TOKEN"
