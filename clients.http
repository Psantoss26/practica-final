### Crear cliente
curl -X POST http://localhost:3000/api/client \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "nombre": "Cliente Demo",
    "email": "cliente@demo.com",
    "telefono": "123456789",
    "direccion": "Calle Falsa 123"
  }'

### Obtener todos los clientes
curl -X GET http://localhost:3000/api/client \
  -H "Authorization: Bearer $TOKEN"

### Obtener un cliente por ID
curl -X GET http://localhost:3000/api/client/ID_DEL_CLIENTE \
  -H "Authorization: Bearer $TOKEN"

### Actualizar cliente
curl -X PATCH http://localhost:3000/api/client/ID_DEL_CLIENTE \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "nombre": "Cliente Actualizado",
    "email": "nuevo@correo.com",
    "telefono": "987654321",
    "direccion": "Otra dirección 456"
  }'

### Archivar cliente (soft delete)
curl -X PATCH http://localhost:3000/api/client/ID_DEL_CLIENTE/archive \
  -H "Authorization: Bearer $TOKEN"

### Eliminar cliente (hard delete)
curl -X DELETE http://localhost:3000/api/client/ID_DEL_CLIENTE \
  -H "Authorization: Bearer $TOKEN"

### Obtener clientes archivados
curl -X GET http://localhost:3000/api/client/archived/list \
  -H "Authorization: Bearer $TOKEN"

### Recuperar cliente archivado
curl -X PATCH http://localhost:3000/api/client/ID_DEL_CLIENTE/recover \
  -H "Authorization: Bearer $TOKEN"
